# Sample inventory. Copy to scripts-orchestrator/inventory/hosts.conf and edit.
# Format: [<alias>] then key=value lines. One blank line between hosts (optional).

[k8s-master]
hostname=192.168.0.20
user=alim
port=22
role=control-plane
group=cluster

[k8s-worker-1]
hostname=192.168.0.21
user=alim
port=22
role=worker
group=cluster

[k8s-worker-2]
hostname=192.168.0.22
user=alim
port=22
role=worker
group=cluster
