17-install-powershell
=====================
let's start now 2026-04-26 (Asia/Kuala_Lumpur)

Installs PowerShell (pwsh) on Ubuntu/Debian.
Resolution order:
  1. Microsoft apt repo (preferred)
  2. snap install powershell --classic (fallback)

Verify: pwsh --version
